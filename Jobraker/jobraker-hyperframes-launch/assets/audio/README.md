# Audio Placeholders

No audio files are referenced by the composition yet, so renders remain deterministic and do not fail on missing assets.

Suggested production replacements:

- `music-bed.wav`: dark kinetic launch bed, 42 seconds.
- `slam-hit.wav`: rejection card impacts in Scene 0.
- `scan-pass.wav`: terminal and proof-graph scan transitions.
- `approve-click.wav`: Scene 3 human-review approval moment.
- `final-lock.wav`: Scene 5 hero lockup.
