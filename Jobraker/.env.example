# Vite-style public envs (required)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_TURNSTILE_SITE_KEY=

# Examples
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_TURNSTILE_SITE_KEY=your_cloudflare_turnstile_site_key

# Backend API Configuration
VITE_API_BASE_URL=https://jobraker-backend.onrender.com/api/v1

# Application Configuration
VITE_APP_NAME=JobRaker
VITE_APP_VERSION=1.0.0
VITE_APP_ENVIRONMENT=production
VITE_APP_URL=https://app.jobraker.io

# Analytics & Monitoring (Optional)
VITE_POSTHOG_KEY=phc_your_project_key
VITE_POSTHOG_HOST=https://us.i.posthog.com
VITE_ANALYTICS_ID=your_analytics_id
VITE_SENTRY_DSN=your_sentry_dsn

# Feature Flags
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_NOTIFICATIONS=true
VITE_ENABLE_REAL_TIME=true

# Legacy Composio Integration (Optional)
VITE_COMPOSIO_GMAIL_CONFIG_ID=your_composio_gmail_config_id

# Gmail API Integration (server-side Edge Function secrets)
# Configure the Google OAuth client redirect URI as:
# https://your-domain.com/auth/callback/gmail
GOOGLE_GMAIL_CLIENT_ID=your_google_oauth_client_id
GOOGLE_GMAIL_CLIENT_SECRET=your_google_oauth_client_secret
GMAIL_TOKEN_ENCRYPTION_KEY=generate_a_long_random_secret

# MCP / Supabase Introspection (Not exposed to client; set only locally)
SUPABASE_PROJECT_REF=your_project_ref
SUPABASE_ACCESS_TOKEN=your_personal_access_token_do_not_commit

# Optional: MCP server tuning
# SUPABASE_MCP_READ_ONLY=true
# SUPABASE_MCP_SCHEMAS=public
# Usage: in your MCP config reference ${SUPABASE_ACCESS_TOKEN}

# (Do not add real secrets here; copy this file to .env and fill values locally.)

# Security & Rotation Guidance
# 1. If any Supabase anon key was exposed publicly, rotate it in the Supabase dashboard (Project Settings -> API -> Regenerate anon key) and update Vercel env + redeploy.
# 2. NEVER commit SUPABASE_ACCESS_TOKEN (service / personal access tokens). Use it only locally or in CI secrets manager.
# 3. For local migrations: run `supabase db push` after adding new migration files under backend/supabase/migrations.
# 4. In production (Vercel): set VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY in Project Settings -> Environment Variables. Rebuild after changes.
# 5. If using Edge Functions or Service Role operations, keep service role key ONLY in server-side environment (never prefixed with VITE_).

# Provider credit monitoring alerts (server-side Edge Function secrets)
FIRECRAWL_API_KEY=your_firecrawl_api_key
SKYVERN_API_KEY=your_skyvern_api_key
RESEND_API_KEY=your_resend_api_key
RESEND_FROM_EMAIL="JobRaker Alerts <alerts@yourdomain.com>"
RESEND_OWNER_EMAIL=you@yourdomain.com
RESEND_OWNER_ONLY=true
PROVIDER_CREDIT_ALERT_EMAIL=admin@yourdomain.com

# PostHog Edge Function secrets (set in Supabase, do not commit real keys)
# supabase secrets set POSTHOG_PROJECT_API_KEY=phc_your_project_key
# supabase secrets set POSTHOG_HOST=https://us.i.posthog.com
