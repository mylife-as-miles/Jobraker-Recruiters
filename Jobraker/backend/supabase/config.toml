# For detailed configuration reference documentation, visit:
# https://supabase.com/docs/guides/local-development/cli/config
# A string used to distinguish different Supabase projects on the same host. Defaults to the
# working directory name when running `supabase init`.
project_id = "backend"

[api]
enabled = true
# Port to use for the API URL.
port = 54321
# Schemas to expose in your API. Tables, views and stored procedures in this schema will get API
# endpoints. `public` and `graphql_public` schemas are included by default.
schemas = ["public", "graphql_public"]
# Extra schemas to add to the search_path of every request.
extra_search_path = ["public", "extensions"]
# The maximum number of rows returns from a view, table, or stored procedure. Limits payload size
# for accidental or malicious requests.
max_rows = 1000

[api.tls]
# Enable HTTPS endpoints locally using a self-signed certificate.
enabled = false

[db]
# Port to use for the local database URL.
port = 54322
# Port used by db diff command to initialize the shadow database.
shadow_port = 54320
# The database major version to use. This has to be the same as your remote database's. Run `SHOW
# server_version;` on the remote database to check.
major_version = 17

[db.pooler]
enabled = false
# Port to use for the local connection pooler.
port = 54329
# Specifies when a server connection can be reused by other clients.
# Configure one of the supported pooler modes: `transaction`, `session`.
pool_mode = "transaction"
# How many server connections to allow per user/database pair.
default_pool_size = 20
# Maximum number of client connections allowed.
max_client_conn = 100

# [db.vault]
# secret_key = "env(SECRET_VALUE)"

[db.migrations]
# If disabled, migrations will be skipped during a db push or reset.
enabled = true
# Specifies an ordered list of schema files that describe your database.
# Supports glob patterns relative to supabase directory: "./schemas/*.sql"
schema_paths = ["./schema.sql"]

[db.seed]
# If enabled, seeds the database after migrations during a db reset.
enabled = true
# Specifies an ordered list of seed files to load during db reset.
# Supports glob patterns relative to supabase directory: "./seeds/*.sql"
sql_paths = ["./seed.sql"]

[db.network_restrictions]
# Enable management of network restrictions.
enabled = false
# List of IPv4 CIDR blocks allowed to connect to the database.
# Defaults to allow all IPv4 connections. Set empty array to block all IPs.
allowed_cidrs = ["0.0.0.0/0"]
# List of IPv6 CIDR blocks allowed to connect to the database.
# Defaults to allow all IPv6 connections. Set empty array to block all IPs.
allowed_cidrs_v6 = ["::/0"]

[realtime]
enabled = true
# Bind realtime via either IPv4 or IPv6. (default: IPv4)
# ip_version = "IPv6"
# The maximum length in bytes of HTTP request headers. (default: 4096)
# max_header_length = 4096

[studio]
enabled = true
# Port to use for Supabase Studio.
port = 54323
# External URL of the API server that frontend connects to.
api_url = "http://127.0.0.1"
# OpenAI API Key to use for Supabase AI in the Supabase Studio.
openai_api_key = "env(OPENAI_API_KEY)"

# Email testing server. Emails sent with the local dev setup are not actually sent - rather, they
# are monitored, and you can view the emails that would have been sent from the web interface.
[inbucket]
enabled = true
# Port to use for the email testing server web interface.
port = 54324
# Uncomment to expose additional ports for testing user applications that send emails.
# smtp_port = 54325
# pop3_port = 54326
# admin_email = "admin@email.com"
# sender_name = "Admin"

[storage]
enabled = true
# The maximum file size allowed (e.g. "5MB", "500KB").
file_size_limit = "50MiB"

# Image transformation API is available to Supabase Pro plan.
# [storage.image_transformation]
# enabled = true

# Uncomment to configure local storage buckets
# [storage.buckets.images]
# public = false
# file_size_limit = "50MiB"
# allowed_mime_types = ["image/png", "image/jpeg"]
# objects_path = "./images"

[auth]
enabled = true
# The base URL of your website. Used as an allow-list for redirects and for constructing URLs used
# in emails.
site_url = "https://app.jobraker.io"
# A list of *exact* URLs that auth providers are permitted to redirect to post authentication.
additional_redirect_urls = [
	"http://127.0.0.1:5173",
	"http://localhost:5173",
	"http://127.0.0.1:3000",
	"http://localhost:3000",
	"https://app.jobraker.io",
	"https://app.jobraker.io/signIn",
	"https://app.jobraker.io/signin",
	"https://app.jobraker.io/login",
	"https://app.jobraker.io/dashboard",
	"https://app.jobraker.io/reset-password",
	"https://www.jobraker.io",
	"https://www.jobraker.io/signIn",
	"https://www.jobraker.io/signin",
	"https://www.jobraker.io/login",
	"https://www.jobraker.io/dashboard",
	"https://www.jobraker.io/reset-password",
	"https://jobraker.io",
	"https://jobraker.io/signIn",
	"https://jobraker.io/signin",
	"https://jobraker.io/login",
	"https://jobraker.io/dashboard",
	"https://jobraker.io/reset-password",
	"https://jobraker-tau.vercel.app",
	"https://jobraker-tau.vercel.app/signIn",
	"https://jobraker-tau.vercel.app/signin",
	"https://jobraker-tau.vercel.app/login",
	"https://jobraker-tau.vercel.app/dashboard",
	"https://jobraker-tau.vercel.app/reset-password"
]
# How long tokens are valid for, in seconds. Defaults to 3600 (1 hour), maximum 604,800 (1 week).
jwt_expiry = 3600
# Path to JWT signing key. DO NOT commit your signing keys file to git.
# signing_keys_path = "./signing_keys.json"
# If disabled, the refresh token will never expire.
enable_refresh_token_rotation = true
# Allows refresh tokens to be reused after expiry, up to the specified interval in seconds.
# Requires enable_refresh_token_rotation = true.
refresh_token_reuse_interval = 10
# Allow/disallow new user signups to your project.
enable_signup = true
# Allow/disallow anonymous sign-ins to your project.
enable_anonymous_sign_ins = false
# Allow/disallow testing manual linking of accounts
enable_manual_linking = false
# Passwords shorter than this value will be rejected as weak. Minimum 6, recommended 12 or more.
minimum_password_length = 12
# Passwords that do not meet the following requirements will be rejected as weak. Supported values
# are: `letters_digits`, `lower_upper_letters_digits`, `lower_upper_letters_digits_symbols`
password_requirements = "lower_upper_letters_digits_symbols"

[auth.rate_limit]
# Number of emails that can be sent per hour. Requires auth.email.smtp to be enabled.
email_sent = 2
# Number of SMS messages that can be sent per hour. Requires auth.sms to be enabled.
sms_sent = 30
# Number of anonymous sign-ins that can be made per hour per IP address. Requires enable_anonymous_sign_ins = true.
anonymous_users = 30
# Number of sessions that can be refreshed in a 5 minute interval per IP address.
token_refresh = 150
# Number of sign up and sign-in requests that can be made in a 5 minute interval per IP address (excludes anonymous users).
sign_in_sign_ups = 30
# Number of OTP / Magic link verifications that can be made in a 5 minute interval per IP address.
token_verifications = 30
# Number of Web3 logins that can be made in a 5 minute interval per IP address.
web3 = 30

# Configure one of the supported captcha providers: `hcaptcha`, `turnstile`.
# [auth.captcha]
# enabled = true
# provider = "hcaptcha"
# secret = ""

[auth.email]
# Allow/disallow new user signups via email to your project.
enable_signup = true
# If enabled, a user will be required to confirm any email change on both the old, and new email
# addresses. If disabled, only the new email is required to confirm.
double_confirm_changes = true
# If enabled, users need to confirm their email address before signing in.
enable_confirmations = true
# If enabled, users will need to reauthenticate or have logged in recently to change their password.
secure_password_change = true
# Controls the minimum amount of time that must pass before sending another signup confirmation or password reset email.
max_frequency = "1s"
# Number of characters used in the email OTP.
otp_length = 6
# Number of seconds before the email OTP expires (defaults to 1 hour).
otp_expiry = 900

# Use a production-ready SMTP server
# [auth.email.smtp]
# enabled = true
# host = "smtp.sendgrid.net"
# port = 587
# user = "apikey"
# pass = "env(SENDGRID_API_KEY)"
# admin_email = "admin@email.com"
# sender_name = "Admin"

# Customize email templates (local dev; mirror in prod Project Settings)
[auth.email.template.confirmation]
subject = "Welcome to Jobraker — Confirm your email"
content_path = "./supabase/templates/confirm.html"

[auth.sms]
# Allow/disallow new user signups via SMS to your project.
enable_signup = false
# If enabled, users need to confirm their phone number before signing in.
enable_confirmations = false
# Template for sending OTP to users
template = "Your code is {{ .Code }}"
# Controls the minimum amount of time that must pass before sending another sms otp.
max_frequency = "5s"

# Use pre-defined map of phone number to OTP for testing.
# [auth.sms.test_otp]
# 4152127777 = "123456"

# Configure logged in session timeouts.
# [auth.sessions]
# Force log out after the specified duration.
# timebox = "24h"
# Force log out if the user has been inactive longer than the specified duration.
# inactivity_timeout = "8h"

# This hook runs before a new user is created and allows developers to reject the request based on the incoming user object.
# [auth.hook.before_user_created]
# enabled = true
# uri = "pg-functions://postgres/auth/before-user-created-hook"

# This hook runs before a token is issued and allows you to add additional claims based on the authentication method used.
# [auth.hook.custom_access_token]
# enabled = true
# uri = "pg-functions://<database>/<schema>/<hook_name>"

# Configure one of the supported SMS providers: `twilio`, `twilio_verify`, `messagebird`, `textlocal`, `vonage`.
[auth.sms.twilio]
enabled = false
account_sid = ""
message_service_sid = ""
# DO NOT commit your Twilio auth token to git. Use environment variable substitution instead:
auth_token = "env(SUPABASE_AUTH_SMS_TWILIO_AUTH_TOKEN)"

# Multi-factor-authentication is available to Supabase Pro plan.
[auth.mfa]
# Control how many MFA factors can be enrolled at once per user.
max_enrolled_factors = 10

# Control MFA via App Authenticator (TOTP)
[auth.mfa.totp]
enroll_enabled = false
verify_enabled = false

# Configure MFA via Phone Messaging
[auth.mfa.phone]
enroll_enabled = false
verify_enabled = false
otp_length = 6
template = "Your code is {{ .Code }}"
max_frequency = "5s"

# Configure MFA via WebAuthn
# [auth.mfa.web_authn]
# enroll_enabled = true
# verify_enabled = true

# Use an external OAuth provider. The full list of providers are: `apple`, `azure`, `bitbucket`,
# `discord`, `facebook`, `github`, `gitlab`, `google`, `keycloak`, `linkedin_oidc`, `notion`, `twitch`,
# `twitter`, `slack`, `spotify`, `workos`, `zoom`.
[auth.external.apple]
enabled = false
client_id = ""
# DO NOT commit your OAuth provider secret to git. Use environment variable substitution instead:
secret = "env(SUPABASE_AUTH_EXTERNAL_APPLE_SECRET)"
# Overrides the default auth redirectUrl.
redirect_uri = ""
# Overrides the default auth provider URL. Used to support self-hosted gitlab, single-tenant Azure,
# or any other third-party OIDC providers.
url = ""
# If enabled, the nonce check will be skipped. Required for local sign in with Google auth.
skip_nonce_check = false

# Allow Solana wallet holders to sign in to your project via the Sign in with Solana (SIWS, EIP-4361) standard.
# You can configure "web3" rate limit in the [auth.rate_limit] section and set up [auth.captcha] if self-hosting.
[auth.web3.solana]
enabled = false

# Use Firebase Auth as a third-party provider alongside Supabase Auth.
[auth.third_party.firebase]
enabled = false
# project_id = "my-firebase-project"

# Use Auth0 as a third-party provider alongside Supabase Auth.
[auth.third_party.auth0]
enabled = false
# tenant = "my-auth0-tenant"
# tenant_region = "us"

# Use AWS Cognito (Amplify) as a third-party provider alongside Supabase Auth.
[auth.third_party.aws_cognito]
enabled = false
# user_pool_id = "my-user-pool-id"
# user_pool_region = "us-east-1"

# Use Clerk as a third-party provider alongside Supabase Auth.
[auth.third_party.clerk]
enabled = false
# Obtain from https://clerk.com/setup/supabase
# domain = "example.clerk.accounts.dev"

[edge_runtime]
enabled = true
# Configure one of the supported request policies: `oneshot`, `per_worker`.
# Use `oneshot` for hot reload, or `per_worker` for load testing.
policy = "oneshot"
# Port to attach the Chrome inspector for debugging edge functions.
inspector_port = 8083
# The Deno major version to use.
deno_version = 1

# [edge_runtime.secrets]
# secret_key = "env(SECRET_VALUE)"

# Example: schedule jobs-cron every 6 hours by setting an env var in project settings
# JOBS_CRON_EXPR = "0 */6 * * *"

[analytics]
enabled = true
port = 54327
# Configure one of the supported backends: `postgres`, `bigquery`.
backend = "postgres"

# Experimental features may be deprecated any time
[experimental]
# Configures Postgres storage engine to use OrioleDB (S3)
orioledb_version = ""
# Configures S3 bucket URL, eg. <bucket_name>.s3-<region>.amazonaws.com
s3_host = "env(S3_HOST)"
# Configures S3 bucket region, eg. us-east-1
s3_region = "env(S3_REGION)"
# Configures AWS_ACCESS_KEY_ID for S3 bucket
s3_access_key = "env(S3_ACCESS_KEY)"
# Configures AWS_SECRET_ACCESS_KEY for S3 bucket
s3_secret_key = "env(S3_SECRET_KEY)"

[functions.skyvern-webhook]
enabled = true
verify_jwt = false
entrypoint = "./functions/skyvern-webhook/index.ts"

[functions.proxy-resume]
enabled = true
verify_jwt = false
entrypoint = "./functions/proxy-resume/index.ts"

# Public image proxy for <img src>; browser requests cannot send Authorization.
[functions.proxy-image]
enabled = true
verify_jwt = false
entrypoint = "./functions/proxy-image/index.ts"

[functions.public-profile-site]
enabled = true
# Public recruiter-facing profile payload. The function itself filters to enabled sites.
verify_jwt = false
entrypoint = "./functions/public-profile-site/index.ts"

[functions.referrals-agent]
enabled = true
# Must be false so browser OPTIONS preflight reaches the handler; auth is enforced in requireSubscriptionTier.
verify_jwt = false
entrypoint = "./functions/referrals-agent/index.ts"

[functions.ai-chat]
enabled = true
# Must be false so browser OPTIONS preflight reaches the handler; auth is enforced in requireSubscriptionTier.
verify_jwt = false
entrypoint = "./functions/ai-chat/index.ts"

[functions.parse-resume]
enabled = true
# Must be false so browser OPTIONS preflight reaches the handler; auth is enforced in requireAuthenticatedUser.
verify_jwt = false
entrypoint = "./functions/parse-resume/index.ts"

[functions.gmail-auth]
enabled = true
# Must be false so browser OPTIONS preflight reaches the handler; auth is enforced in requireSubscriptionTier.
verify_jwt = false
entrypoint = "./functions/gmail-auth/index.ts"

[functions.sync-gmail-application-events]
enabled = true
# Must be false so browser OPTIONS preflight reaches the handler; auth is enforced in requireSubscriptionTier.
verify_jwt = false
entrypoint = "./functions/sync-gmail-application-events/index.ts"

[functions.provider-credits]
enabled = true
# Browser OPTIONS preflight must reach the handler; admin auth is enforced inside the function.
verify_jwt = false
entrypoint = "./functions/provider-credits/index.ts"

[functions.list-users]
enabled = true
# Browser OPTIONS preflight must reach the handler; admin auth is enforced inside the function.
verify_jwt = false
entrypoint = "./functions/list-users/index.ts"

[functions.admin-delete-user]
enabled = true
# Browser OPTIONS preflight must reach the handler; admin auth is enforced inside the function.
verify_jwt = false
entrypoint = "./functions/admin-delete-user/index.ts"

[functions.calculate-match-score]
enabled = true
# Browser OPTIONS preflight must reach the handler; auth is enforced in requireSubscriptionTier.
verify_jwt = false
import_map = "./functions/calculate-match-score/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/calculate-match-score/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/calculate-match-score/*.html" ]

[functions.generate-cover-letter]
enabled = true
# Browser OPTIONS preflight must reach the handler; auth is enforced in requireSubscriptionTier.
verify_jwt = false
import_map = "./functions/generate-cover-letter/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/generate-cover-letter/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/generate-cover-letter/*.html" ]

[functions.generate-outreach]
enabled = true
# Browser OPTIONS preflight must reach the handler; auth is enforced in requireSubscriptionTier.
verify_jwt = false
import_map = "./functions/generate-outreach/deno.json"
entrypoint = "./functions/generate-outreach/index.ts"

[functions.scout-company]
enabled = true
verify_jwt = false
entrypoint = "./functions/scout-company/index.ts"

[functions.send-email]
enabled = true
verify_jwt = false
entrypoint = "./functions/send-email/index.ts"

[functions.tailor-resume]
enabled = true
# Browser OPTIONS preflight must reach the handler; auth is enforced in requireSubscriptionTier.
verify_jwt = false
import_map = "./functions/tailor-resume/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/tailor-resume/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/tailor-resume/*.html" ]

[functions.evaluate-job-fit]
enabled = true
# Browser OPTIONS preflight must reach the handler; auth is enforced in requireSubscriptionTier.
verify_jwt = false
entrypoint = "./functions/evaluate-job-fit/index.ts"

[functions.apply-to-jobs]
enabled = true
# Browser OPTIONS preflight must reach the handler; auth is enforced in requireSubscriptionTier.
verify_jwt = false
entrypoint = "./functions/apply-to-jobs/index.ts"

[functions.jobs-search]
enabled = true
# Browser OPTIONS preflight must reach the handler; auth is enforced in requireAuthenticatedUser.
verify_jwt = false
entrypoint = "./functions/jobs-search/index.ts"

[functions.track-posthog]
enabled = true
# Browser OPTIONS preflight must reach the handler; auth is enforced in requireAuthenticatedUser.
verify_jwt = false
entrypoint = "./functions/track-posthog/index.ts"

[functions.generate-embeddings]
enabled = true
# Browser OPTIONS preflight must reach the handler; auth is enforced in requireAuthenticatedUser.
verify_jwt = false
entrypoint = "./functions/generate-embeddings/index.ts"

[functions.generate-answer-bank]
enabled = true
# Browser OPTIONS preflight must reach the handler; auth is enforced in requireAuthenticatedUser.
verify_jwt = false
entrypoint = "./functions/generate-answer-bank/index.ts"

[functions.customer-support-chat]
enabled = true
# Browser OPTIONS preflight must reach the handler; auth is enforced in requireAuthenticatedUser.
verify_jwt = false
entrypoint = "./functions/customer-support-chat/index.ts"

[functions.ai-generate-resume]
enabled = true
verify_jwt = true
entrypoint = "./functions/ai-generate-resume/index.ts"

[functions.analyze-resume]
enabled = true
verify_jwt = true
entrypoint = "./functions/analyze-resume/index.ts"

[functions.composio-gmail-auth]
enabled = true
verify_jwt = true
entrypoint = "./functions/composio-gmail-auth/index.ts"

[functions.enrich-jobs]
enabled = true
verify_jwt = true
entrypoint = "./functions/enrich-jobs/index.ts"

[functions.firecrawl-health]
enabled = true
verify_jwt = true
entrypoint = "./functions/firecrawl-health/index.ts"

[functions.generate-chat-starters]
enabled = true
verify_jwt = true
entrypoint = "./functions/generate-chat-starters/index.ts"

[functions.generate-title]
enabled = true
verify_jwt = true
entrypoint = "./functions/generate-title/index.ts"

[functions.get-extract-status]
enabled = true
verify_jwt = true
entrypoint = "./functions/get-extract-status/index.ts"

[functions.get-jobs]
enabled = true
verify_jwt = true
entrypoint = "./functions/get-jobs/index.ts"

[functions.get-run]
enabled = true
verify_jwt = true
entrypoint = "./functions/get-run/index.ts"

[functions.init-payment]
enabled = true
verify_jwt = true
entrypoint = "./functions/init-payment/index.ts"

[functions.intake-job-url]
enabled = true
verify_jwt = true
entrypoint = "./functions/intake-job-url/index.ts"

[functions.interview-session]
enabled = true
verify_jwt = true
entrypoint = "./functions/interview-session/index.ts"

[functions.jobs-cron]
enabled = true
verify_jwt = true
entrypoint = "./functions/jobs-cron/index.ts"

[functions.paystack-webhook]
enabled = true
# Paystack webhooks cannot include a Supabase JWT; signature validation happens in the function.
verify_jwt = false
entrypoint = "./functions/paystack-webhook/index.ts"

[functions.polish-content]
enabled = true
verify_jwt = true
entrypoint = "./functions/polish-content/index.ts"

[functions.process-and-match]
enabled = true
verify_jwt = true
entrypoint = "./functions/process-and-match/index.ts"

[functions.process-auto-apply-queue]
enabled = true
verify_jwt = false
entrypoint = "./functions/process-auto-apply-queue/index.ts"

[functions.process-job-search]
enabled = true
verify_jwt = true
entrypoint = "./functions/process-job-search/index.ts"

[functions.process-task]
enabled = true
verify_jwt = false
entrypoint = "./functions/process-task/index.ts"

[functions.repair-payment]
enabled = true
verify_jwt = false
entrypoint = "./functions/repair-payment/index.ts"

[functions.request-early-access]
enabled = true
verify_jwt = true
entrypoint = "./functions/request-early-access/index.ts"

[functions.request-enterprise-sales]
enabled = true
verify_jwt = true
entrypoint = "./functions/request-enterprise-sales/index.ts"

[functions.schedule-interview]
enabled = true
verify_jwt = true
import_map = "./functions/schedule-interview/deno.json"
entrypoint = "./functions/schedule-interview/index.ts"

[functions.sync-provider-status]
enabled = true
verify_jwt = true
entrypoint = "./functions/sync-provider-status/index.ts"

[functions.verify-payment]
enabled = true
verify_jwt = true
entrypoint = "./functions/verify-payment/index.ts"
